# RCU 学习地图

## 第一章：入口更新后旧对象为什么仍不能释放

从一个“发布正确、回收错误”的多 CPU 交错现场区分入口更新与对象生命期。

## 第二章：宽限期究竟等待谁

从持续到来的新读者反推一个有边界的旧读者集合，排除固定延时和全局无人读取的误解。

## 第三章：读侧成本转移到了哪里

比较共享计数和 RCU，明确发布、等待、回调、写者串行化与对象逃逸责任。

## 第四章：Tree RCU 怎样汇聚完成证据

把抽象旧读者边界映射到局部状态、`rcu_node` 层次汇聚、抢占登记和超时慢路径。
