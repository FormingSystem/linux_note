---
id: tools.practice_tool.architecture.implementation_status
title: "回路实现状态与版本边界"
kind: reference
status: evolving
domains:
  - tools
---

# 第1章\_回路实现状态与版本边界

## 1.1\_文档职责

本文是设计与代码之间的实现状态单一事实来源。产品设计文档描述目标行为，本文件记录当前代码已经兑现的能力、部分兑现的约束、尚未实现的能力和验证证据。不得因为页面出现入口就把占位页视为功能完成。

当前基线对应 `tools/practice_tool` 的 `0.1.0` 开发版本，更新时间为 2026-07-26。

## 1.2\_当前可用主流程

```mermaid
flowchart LR
    L[大厅] --> LIB[训练库]
    LIB --> D[单元详情]
    D --> S[创建或恢复会话]
    S --> LEARN[学习导引]
    LEARN --> GUIDE[提示提问]
    GUIDE --> MODEL[脱稿输出]
    MODEL --> CASE[专业案例]
    CASE --> SUMMARY[训练总结]
    S --> L
```

当前提供 RCU、红黑树和哈希表三个训练单元，共 15 个训练任务。单元由 `banks/index.json` 和单元协议发现，页面没有按专题硬编码分支。

## 1.3\_已经实现

### 1.3.1\_应用导航

- 大厅、训练库、单元详情、我的训练和训练会话已经使用独立路由。
- 主导航提供大厅、训练库、我的训练、复习、历史、知识源和设置入口。
- 训练页提供返回大厅入口。
- 浏览器前进、后退和刷新可以按照 URL 恢复页面。
- 无效顶层路由返回大厅；不存在的单元和会话显示错误状态。

### 1.3.2\_训练会话

- 新会话保存完整训练内容快照，不随题库当前内容静默变化。
- URL 使用会话 ID、阶段和稳定题目 ID，不使用数组序号。
- 学习导引、提示提问、脱稿输出和专业案例按顺序解锁。
- 已解锁阶段可以从步骤栏进入；手工修改 URL 不能绕过阶段锁。
- 支持上一项、完成并继续、自评、逐级提示、答案核验和训练总结。
- 大厅展示最近三个进行中会话，可以恢复到上次题目。

### 1.3.3\_本地持久化

- 训练会话、答案、提示层级、自评、已揭示内容和完成位置保存在 IndexedDB。
- 输入变更采用 1000 毫秒防抖自动保存，并显示待保存、保存中、已保存或失败状态。
- 保存失败时可以重试或导出当前会话草稿。
- 分类和训练模块保存在 IndexedDB。
- 旧版工作区 `localStorage` 数据首次读取后迁移到 IndexedDB，并删除旧键。

### 1.3.4\_训练组织

- 分类支持创建、修改、上下级组织、移入回收站、恢复和合并。
- 分类父级选项排除自身和后代，避免形成层级环。
- 训练模块支持创建、修改、选择多个训练单元、关联多个分类、回收、恢复和合并。
- 工作区支持版本化 JSON 导入和导出。
- 管理操作使用页面内正式表单，不使用浏览器 `prompt()`。

### 1.3.5\_现有系统能力

- 知识来源仍通过受控只读接口打开注册知识源内的原文。
- 应用外壳保留版本查询、更新提示和安全更新入口。
- 搜索、领域筛选、空状态、加载状态和基础响应式布局已经提供。

## 1.4\_部分实现

| 能力 | 当前状态 | 仍需补齐 |
| --- | --- | --- |
| 返回大厅 | 可以返回，组件卸载时保存最新内存状态 | 导航前等待强制保存结果；失败时提供留在页面、导出后离开和明确放弃 |
| 会话状态机 | 已实现 `in_progress`、`completed` 数据 | `draft`、`paused`、`abandoned` 的完整事件和审计记录 |
| 路由守卫 | 已检查会话、单元、题目和阶段解锁 | 已回收会话、知识源离线诊断和统一错误码 |
| 自动保存 | 防抖、状态提示、重试和草稿导出 | 修订日志、崩溃恢复测试和保存队列合并 |
| 工作区导入 | 格式和基本结构校验 | 预览、逐项冲突策略、事务回滚、摘要和来源映射 |
| 无障碍 | 使用语义按钮、标签、状态区域和键盘原生控件 | 焦点管理、跳转链接、对话框焦点陷阱、NVDA 验收 |
| 移动端 | 核心页面能够单列显示 | 设计中的五项底部导航和专用训练入口 |

## 1.5\_尚未实现

- 复习调度、到期队列、薄弱项重练和掌握度模型。
- 可筛选、可比较、可导出的训练历史页面。
- 知识源管理页面和来源可用性诊断。
- 设置项、数据清理、备份恢复和隐私控制页面。
- 用户置顶会话、多个会话的完整管理和“重新开始”冲突处理。
- 多标签页单写者租约、围栏令牌和只读接管。
- 撤销提示条、永久删除和回收站保留期限。
- 内容适配草稿、人工审核、发布和 AI 治理工作流。
- 自动化组件测试、端到端浏览器测试、性能基准和无障碍工具验收。

当前复习、历史、知识源和设置路由只提供边界说明占位页，不属于功能完成。

## 1.6\_当前代码结构

```text
src/
├── app/
│   ├── App.tsx
│   └── AppShell.tsx
├── features/
│   ├── lobby/
│   ├── practice-sessions/
│   ├── training-library/
│   └── training-management/
├── infrastructure/
│   └── persistence/
├── shared/
│   ├── components/
│   └── types.ts
├── styles/
└── main.tsx
```

这是第一版已经落地的 Feature-first 骨架，不是最终拆分完成状态。`practice-sessions/SessionPage.tsx` 和 `training-management/WorkspaceManager.tsx` 仍同时承担较多界面与用例编排职责，后续应按各自模块内部约定继续拆分，不得重新塞回 `App.tsx`。

## 1.7\_验证基线

当前已通过：

```text
npm run check:data
  3 个训练单元
  15 个训练任务
  索引、稳定 ID 和知识引用有效

npm run build
  TypeScript 类型检查通过
  Vite 生产构建通过

HTTP 路由探测
  / 返回 200
  /library/units/linux.data_structures.rbtree 返回 200

git diff --check
  通过
```

尚未完成真实浏览器自动化点击验收，因为当次验证环境没有可用浏览器控制实例。该限制必须保留在交付记录中，不能把构建成功表述成完整交互验收成功。

## 1.8\_已知风险

`react-router-dom` 固定为 `7.18.1`。当前依赖审计报告 2 个高危项，来自 React Router 的 RSC 模式 CSRF 公告；回路当前是纯浏览器 Vite 应用，没有启用 RSC 或服务端 Action。审计建议的降级版本同时命中更多已披露漏洞，因此没有执行强制降级。后续应跟踪上游安全版本并重新审计。

## 1.9\_下一实现顺序

1. 完成返回大厅前的可等待强制保存和失败决策。
2. 拆分训练会话用例、步骤组件和存储接口，增加单元测试。
3. 实现训练历史和薄弱项复习，使总结页形成闭环。
4. 增加多标签页租约、修订日志和恢复测试。
5. 完成导入预览、冲突处理和事务回滚。
6. 执行真实浏览器、键盘、NVDA、响应式和性能验收。

## 1.10\_相关设计

- [产品导航与交互设计](product/navigation_and_interaction.md)
- [训练会话状态与持久化](product/training_session_state_and_persistence.md)
- [工程结构与模块边界](engineering/project_structure_and_module_boundaries.md)
- [无障碍、性能与产品验收标准](engineering/accessibility_performance_and_acceptance.md)
