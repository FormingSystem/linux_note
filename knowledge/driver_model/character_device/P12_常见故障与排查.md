---
id: knowledge.driver_model.character_device.p01_设备号框架简介.p04_深入与排错_把坑一次性踩完
title: "字符设备常见故障与排查"
kind: subsystem
status: evolving
domains:
  - linux
  - kernel
  - driver
---

# 第12章\_字符设备常见故障与排查

## 12.1\_/dev/demo0\_是普通文件\_不是字符设备

```bash
ls -l /dev/demo0
# 若最左列是 '-'（普通文件）而不是 'c'（字符设备），说明你曾经 touch 过
sudo rm -f /dev/demo0
```

重新创建（或用自动节点）。

## 12.2\_echo\_>\_/dev/demo0\_没任何\_printk

- 核对节点设备号：

  ```bash
  stat -c "major=%t minor=%T" /dev/demo0
  ```

  要和 `dmesg` 里打印的 major、minor 一致。

- 确认模块已加载、未卸载：`lsmod | grep demo`

## 12.3\_rmmod:\_Module\_demo\_is\_in\_use

- 有进程没关设备：

  ```bash
  fuser /dev/demo0
  lsof /dev/demo0
  ```

- 关掉占用进程再卸载。

## 12.4\_同名模块/设备号冲突

- 静态 major 要先查 `/proc/devices`，避免撞车；
- 动态分配一般不冲突；
- 模块名冲突时，第二次 `insmod` 会报 `File exists`。

## 12.5\_让\_udev\_给你固定权限/固定软链名

`/etc/udev/rules.d/90-demo.rules`：

```
KERNEL=="demo[0-9]*", MODE="0666", SYMLINK+="mydemo%n"
```

重载：

```bash
sudo udevadm control --reload
sudo udevadm trigger
```

效果：`/dev/demo0` 同时出现 `/dev/mydemo0` 软链，权限按规则设置。
