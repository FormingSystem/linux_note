---
id: knowledge.driver_model.input.input_子系统简介.大纲
title: "Linux Input 子系统实战 从零到驱动上线 大纲(最新版)"
kind: subsystem
status: evolving
domains:
  - linux
  - kernel
  - driver
---

# 第1章_Linux_Input_子系统实战_从零到驱动上线_大纲(最新版)

## 1.1_学前说明与成果物

- 目标与阅读路径：10 分钟跑通、1 小时理解流程、1 天写出可用驱动
- 环境与工具：内核版本、交叉编译、`evtest`、`libinput debug-events`
- 代码风格约定：**一切具现实语义的数值一律用具名宏/常量（含单位后缀）**；中文注释；K&R/tab=4

## 1.2_入门心智模型(不提_API_也能看懂)

- 现实痛点 → Input 要解决什么（噪声/越界/半帧/多消费者/策略归属/功耗）
- 四角色与边界：硬件（产样本）→ 驱动（采样成帧+声明边界）→ input core（钳位/抑抖/分发）→ 用户态（手势/空间策略）
- 端到端时序：IRQ/轮询 → 采样 → 成帧 → 分发 → 用户态读取（帧语义）

## 1.3_体系结构总览

- 设备 `input_dev`、核心、处理层（evdev/joydev/mousedev）、用户态关系图
- UAPI 稳定与“低策略”原则：为何策略放用户态
- 单点 vs 多点：MT Protocol A/B（重点 B 的“槽位/帧”）

## 1.4_先跑起来_最小可运行输入设备(Hello,_Input)

- 最小设备注册三件套（devres/非 devres对比）
- 3 帧虚拟事件：按下→移动→抬起（**上报不睡**、**帧结束语义**）
- `evtest` 验收与 `EVIOCGABS` 读取元数据

## 1.5_两条完整流水线(问题_to_作用_to_定位_to_细节)

- **LCD 电容触摸屏（I²C，多点，DIRECT）**
  - 噪声/越界/同帧一致/多消费者 → 用 **`min/max/fuzz/res` + 帧语义** 解决
  - 用户态做手势与坐标变换
- **ADC 双轴摇杆**
  - 机械回中抖、死区 → **`fuzz` 抑抖** + **`flat` 建议死区（用户态采纳）**

## 1.6_同步与并发语义(必须讲透)

- **采集可睡、上报不睡**：IRQ 上下文、线程化 IRQ、workqueue、进程上下文
- 设备级顺序：input core 短自旋锁；处理层环形缓冲 + 唤醒
- 帧一致性与 TOCTOU：帧号或“前后检查 data-ready + 有限重读”
- 关机/休眠顺序：`disable_irq_sync()` → 改电源/状态；唤醒源配置
- 背压与丢帧提示：用户态自愈重同步流程

## 1.7_API_家族总览与_四连问_方法论

> 每个 API 都按：**作用 / 使用场景 / 不写或写错的后果 / 驱动落点（具名宏示例）**

- 6.1 设备生命周期：分配/注册/注销；open/close；devres vs 非 devres
- 6.2 能力与属性声明：`evbit/keybit/absbit/propbit`，DIRECT/POINTER
- 6.3 绝对轴参数族（**重点**）：`min/max/fuzz/flat/res` 与查询/联动
- 6.4 多点触控（MT-B）：槽位初始化、槽状态、帧同步、常见坑
- 6.5 事件上报：按键/绝对/相对/开关 + `SYN_REPORT`（帧结束）
- 6.6 用户态接口：evdev 基础、关键 `ioctl`、调试工具

## 1.8_案例_A_LCD_触摸屏驱动(完整可编译)

- 参数表（全部具名宏，含单位后缀）与设备树要点
- 线程化 IRQ 取帧 → 成帧 → 上报；**钳位/抑抖由 input core 执行**
- 并发处理（ONESHOT、`disable_irq_sync()`、TOCTOU 对策）
- Bring-up Checklist 与 `evtest` 观测项；常见故障定位

## 1.9_案例_B_ADC_双轴摇杆驱动(完整可编译)

- 标定与映射；`fuzz` 抑机械抖；`flat`（建议死区）面向用户态
- 轮询/阈值中断两种触发；上报与验证；反模式清单

## 1.10_验证_调试与性能

- `/proc/bus/input/devices`、`evtest` 事件解析
- `EVIOCGABS` 校验 `min/max/fuzz/flat/res`
- 动态调试与 tracepoints；IRQ→用户态延迟测量

## 1.11_交付清单与模板库

- Kconfig/Makefile/DTS 模板
- 驱动骨架（含并发/PM/异常路径）
- **具名宏命名规范**：`*_PX/_MM/_CNT/_HZ/_MS` 等

## 1.12_常见误解与_FAQ

- “input 触发硬件中断？”（错）
- “flat 在触摸里生效？”（通常由用户态采纳）
- “在驱动里做手势/平滑更好？”（错，策略归用户态）

## 1.13_附录

- 术语速查；MT A/B 差异；处理层简表
- 参考代码跳转索引（内核目录与关键结构体）