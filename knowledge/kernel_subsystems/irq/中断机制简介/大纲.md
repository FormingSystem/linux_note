---
id: knowledge.kernel_subsystems.irq.中断机制简介.大纲
title: "大纲"
kind: subsystem
status: evolving
domains:
  - linux
  - kernel
---



### 1.1.1\_中断的定位与演化

### 1.1.2\_SoC\_视角下的中断体系

### 1.1.3\_Linux\_的中断抽象\_irq\_chip\_irq\_domain\_与\_irq\_desc

### 1.1.4\_Linux\_驱动中的中断注册与接口

### 1.1.5\_GPIO\_与触发语义\_电平\_边沿\_DTS\_与\_只来一次一直来\_为何出现

### 1.1.6\_Linux\_中断的调试与诊断

### 1.1.7\_中断下半部机制与驱动中的选择

### 1.1.8\_中断\_x\_高精度定时器(hrtimer)\_去抖\_延时确认与一次性中断再使能

------

### 1.1.9\_层级中断控制器与\_ARM\_GIC\_在\_Linux\_中的实现

9.1 Linux 为何要搞层级 irq_domain
 9.2 GICv2/v3 在内核里的 irqchip 结构
 9.3 GPIO/外部中断控制器挂到 GIC 的路径还原
 9.4 层级域中断号的申请、映射与传播
 9.5 常见问题：触发类型不生效 / 中断号对不上 / 子控制器没放行

### 1.1.10\_SMP\_与中断亲和性\_IPI\_机制

10.1 多核下中断要解决的核心问题
 10.2 irq affinity、smp_affinity 与 irqbalance
 10.3 IPI（处理器间中断）的类型与内核用途
 10.4 高并发/高 PPS 下的中断分摊策略
 10.5 驱动与中断亲和性的协同写法

### 1.1.11\_电源管理中的中断\_suspend/resume\_与唤醒源

11.1 为什么挂起后中断“不来/一直来”
 11.2 wakeup-source、IRQF_NO_SUSPEND 的语义与差异
 11.3 GPIO 中断唤醒与 pinctrl-sleep 的配合
 11.4 runtime PM 下的中断可用性
 11.5 驱动里的典型写法与排查思路

### 1.1.12\_MSI\_/\_MSI-X\_与\_PCIe\_设备的中断框架

12.1 MSI 出现的动机与和“线中断”的区别
 12.2 Linux MSI 框架与 irq_domain 的结合
 12.3 平台中断 → MSI 的迁移
 12.4 多中断向量设备（MSI-X）的分配与管理
 12.5 嵌入式/SoC 场景下的精简与限制

### 1.1.13\_特殊与高优先级中断\_NMI\_定时器\_性能中断

13.1 NMI 在 Linux 中的入口与约束
 13.2 本地定时器中断与 tick 子系统
 13.3 PMU/性能计数器中断
 13.4 这些中断与普通外设中断的区别
 13.5 对驱动开发者需要知道的边界

### 1.1.14\_中断风暴\_抖动与防御机制

14.1 中断风暴的形成路径
 14.2 内核层面的限流、disable、oneshot 化
 14.3 软中断/工作队列维度的卸压
 14.4 与硬件消抖、hrtimer 二次确认的协同
 14.5 一套通用的故障处理模板

### 1.1.15\_实战模式与可复用模板

15.1 “GPIO 按键 + 唤醒 + 防抖”全链路
15.2 “层级控制器 + DTS 配置错误”的排查模板
15.3 “共享中断不触发/乱触发”的定位流程
15.4 “高频中断性能问题”的排查与优化
15.5 写驱动时的中断接口选择速查表

执行上下文和下半部选择以[第 7 章](P07_中断下半部机制与驱动中的选择.md)为权威入口，工作队列内部机制转入[工作队列专题](../../workqueue/大纲.md)，本节不再重复展开。
