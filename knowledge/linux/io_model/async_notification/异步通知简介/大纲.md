---
id: knowledge.linux.io_model.async_notification.异步通知简介.大纲
title: "异步通知全景与知识地图"
kind: mechanism
status: evolving
domains:
  - linux
  - kernel
---

# 第1章_异步通知全景与知识地图

1.1 本书主题与目标
 1.2 Linux 中的多种“通知机制”概览

- 阻塞/非阻塞 I/O
- select/poll/epoll
- 信号（SIGIO/SIGPOLL 等）
- 专用子系统通知（input、netlink 等）

1.3 驱动异步通知（fasync）的定位与边界
 1.4 本书的假定前提（内核版本、平台、读者基础）
 1.5 学习路线建议与章节依赖关系图

------

# 第2章_Linux_I/O_与信号机制基础

2.1 文件描述符与 VFS 基础回顾
 2.2 阻塞 I/O 与非阻塞 I/O 的行为差异
 2.3 select/poll/epoll 的事件等待模型
 2.4 Linux 信号子系统概要（同步 vs 异步、可重入性、信号队列）
 2.5 SIGIO / SIGPOLL 等与 I/O 事件的关系
 2.6 小结：为什么需要 fasync 这种“驱动级触发 + 信号级传递”的组合

------

# 第3章_fasync_机制的历史与设计动机

3.1 fasync 的历史演进与使用场景
 3.2 “让驱动主动叫醒用户”的设计目标
 3.3 设计约束：兼容文件描述符、信号、进程/进程组
 3.4 相对 select/poll/epoll 的优势与局限
 3.5 为什么在现代项目里仍然值得理解和掌握 fasync
 3.6 本章小结：fasync 机制在整体生态里的角色定位

------

# 第4章_内核视角_核心数据结构与控制路径

4.1 `file->f_flags` 与 `FASYNC` 标志位
 4.2 `struct fasync_struct` 数据结构与链表组织
 4.3 `struct file_operations` 中的 `.fasync` 回调
 4.4 `fasync_helper()` 的工作流程与使用约定
 4.5 `kill_fasync()` 与信号子系统的衔接逻辑
 4.6 进程/进程组所有权：`F_SETOWN`、`F_SETSIG` 与任务结构
 4.7 结合源码的函数调用关系总览

------

# 第5章_VFS_与文件系统中的异步通知支持

5.1 VFS 打开/关闭路径中 `FASYNC` 的传播机制
 5.2 `fcntl(F_SETFL)` 与 `O_ASYNC/FASYNC` 的交互
 5.3 各类文件（常规文件、管道、socket、字符设备）的行为差异
 5.4 字符设备与 `.fasync` 回调的注册与调用时机
 5.5 特定文件系统/驱动中对异步通知的扩展实现概览
 5.6 本章小结：从 VFS 视角看 fasync 是如何“插”进去的

------

# 第6章_字符设备中实现异步通知的基础模式

6.1 面向字符设备的最小 fasync 模板
 6.2 在 `open()` / `release()` 中维护 fasync 状态
 6.3 `.fasync` 回调的标准写法与异常处理
 6.4 在中断处理函数/下半部中调用 `kill_fasync()` 的流程
 6.5 与 `.read()` / `.poll()` 的状态一致性约束
 6.6 devm 风格与非 devm 风格下资源/状态管理思路
 6.7 本章小结：一个“最小可用”字符设备异步通知驱动长什么样

------

# 第7章_中断驱动设备的异步通知实践

7.1 适合使用 fasync 的中断型设备特征
 7.2 GPIO 按键 / 外部事件输入的异步通知设计
 7.3 中断上下文、tasklet、workqueue 与 `kill_fasync()` 的时序选择
 7.4 去抖动、节流与事件聚合策略
 7.5 与 `waitqueue` / `completion` / `poll` 的组合使用模式
 7.6 典型错误模式：在错误上下文调用 `kill_fasync()`、重复通知、丢事件
 7.7 案例：基于 i.MX6ULL GPIO 中断的 demo 异步通知驱动结构

------

# 第8章_流式设备与高级场景中的异步通知

8.1 串口/TTY 等流式设备中的异步通知习惯用法
 8.2 高速采集设备中使用 fasync 的限制与折中方案
 8.3 大量事件场景下的“信号风暴”问题
 8.4 与缓冲区管理、丢帧策略、SYN_DROPPED 类语义的对应关系
 8.5 与 input 子系统通知机制、netlink 通知的对比
 8.6 适合/不适合使用 fasync 的高级场景分类

------

# 第9章_用户态编程_从_SIGIO_到_signalfd/epoll

9.1 `fcntl(F_SETOWN)` / `F_SETSIG` / `F_SETFL(FASYNC)` 的调用顺序
 9.2 使用 `SIGIO` 的经典写法（signal handler + 全局状态）
 9.3 使用 `sigaction` 的可靠信号处理模式
 9.4 使用 `signalfd` 接收 SIGIO，并接入 `epoll` 事件循环
 9.5 多线程/多进程下的所有权、fd 共享和信号路由问题
 9.6 用户态典型误用：顺序错误、信号未解掩、处理函数过重等
 9.7 “现代风格”的用户态事件循环（epoll + signalfd + fasync）的组合示例

------

# 第10章_并发_竞态与内存可见性问题

10.1 fasync 相关状态（链表、标志位）的并发访问问题
 10.2 中断上下文 vs 进程上下文：锁的选择（spinlock/mutex）
 10.3 `kill_fasync()` 与进程信号队列的并发行为
 10.4 fasync 与 `waitqueue`/`poll` 的竞态：谁先感知、如何保持顺序语义
 10.5 fasync 相关状态的释放与“悬垂 fasync_struct”问题
 10.6 内存屏障、原子操作在 fasync 实现中的角色
 10.7 实战中值得显式考虑的竞态场景清单

------

# 第11章_调试_排错与性能分析

11.1 “已经设置 FASYNC 却收不到 SIGIO”的排查步骤
 11.2 查看进程状态：`/proc/PID/status`、`/proc/PID/fd` 等
 11.3 使用 `strace` 观察 fcntl/信号行为
 11.4 使用 `ftrace/trace_printk` 定位 `.fasync` / `kill_fasync()` 调用
 11.5 信号风暴、丢信号、重复通知的典型症状与定位方法
 11.6 延迟测量：从设备事件到用户态处理的时间链路分析
 11.7 性能优化方向：事件聚合、节流、分级通知策略

------

# 第12章_与其他通知机制的对比与整合

12.1 fasync vs select/poll/epoll：适用范围与取舍
 12.2 fasync vs input 子系统事件模型
 12.3 fasync vs netlink/kobject_uevent/sysfs 轮询
 12.4 与 io_uring / AIO 等新型异步 I/O 机制的关系
 12.5 在实际工程中如何选择通知机制：决策矩阵与推荐组合
 12.6 面向未来的演进方向与内核趋势简述

------

# 第13章_工程实践案例与编码核对表

13.1 案例一：GPIO 中断 + fasync + 用户态 SIGIO
 13.2 案例二：多路设备 + signalfd + epoll 统一事件循环
 13.3 案例三：从轮询版驱动改造为 fasync 版驱动的全过程
 13.4 devm 与非 devm 版本的差异与迁移注意点
 13.5 驱动侧开发核对表（必写项/可选项/禁用项）
 13.6 用户态应用核对表（配置步骤/信号处理/退出流程）

------

# 第14章_总结与扩展阅读

14.1 全书知识点结构回顾
 14.2 结合 input 子系统等专题的综合应用建议
 14.3 推荐源码阅读路径（内核版本/目录/关键文件）
 14.4 推荐文档、书籍与社区资源
 14.5 后续可扩展的研究方向（实时系统、安全性、复杂事件框架等）

