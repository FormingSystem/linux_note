---
id: knowledge.linux.synchronization.rcu.outline
title: "RCU 专题大纲"
kind: track
status: evolving
domains:
  - linux
  - kernel
topics:
  - synchronization
  - rcu
  - object_lifetime
---

# 第1章\_RCU\_专题大纲

## 1.1\_专题定位

本专题不按“概念、API、字段”平铺，而是复现 RCU 机制被问题逐步逼出来的过程：先看到锁方案的正确性和扩展瓶颈，再自行推出多版本、发布边界和 GP；掌握最小使用协议后，分别解决非抢占式 CPU 证明与抢占式任务跟踪，最后进入完整 Tree RCU 子系统、回调、变体、应用和验证。

**进入源码前必须先做家族分流。** P03～P21 的 `rcu_read_lock()/synchronize_rcu()`、`rcu_state`、`rcu_node` 和 GP kthread 主线默认讨论 **普通 RCU 在 SMP 上的 Tree RCU 实现**，不是所有名字带 RCU 的机制。若调用点使用显式 `srcu_struct`，reader 允许主动睡眠，应直接先读 [P22 的三个正交维度](P22_RCU_实现家族与内核配置.md#22.2_三个正交维度)和 [P23 SRCU 私有域](P23_SRCU_私有域与双_index_状态机.md#23.1_问题场景_注销监听器时不能释放正在睡眠的回调对象)，再走独立 SRCU 源码分支。`PREEMPT_RCU` 允许普通 reader 被调度器抢占，不等于允许它主动阻塞。

| 看到的入口或约束 | 应进入的主线 |
| --- | --- |
| `rcu_read_lock()`、`synchronize_rcu()`、`call_rcu()` | 普通 RCU；本仓库目标 SMP 配置对应 Tree RCU |
| `srcu_read_lock(&domain)`、reader 要等待 mutex/I/O | SRCU 私有域与双 index 状态机 |
| `synchronize_rcu_tasks*()`、BPF/ftrace 执行轨迹 | Tasks RCU 家族 |
| `CONFIG_TINY_RCU` 单 CPU 构建 | 普通 RCU 语义的 Tiny 底层实现 |

```mermaid
flowchart LR
    A["P01<br/>锁方案的价值与缺口"]
    B["P02<br/>推演发布边界与GP"]
    C["P03<br/>最小API使用闭环"]
    D["P04<br/>RCU与kref生命周期组合"]
    E["P05-P06<br/>非抢占式CPU证明"]
    F["P07-P08<br/>抢占式任务债务"]
    G["P09-P10<br/>现实约束与统一总图"]
    H["P11-P21<br/>Tree RCU实现纵深"]
    I["P22-P24<br/>实现家族与私有域"]
    J["P25-P28<br/>应用、类型、验证与选择"]

    A -->|"为什么不能只靠读写锁"| B
    B -->|"抽象动作怎样落到API"| C
    C -->|"裸指针怎样变成长持有"| D
    D -->|"先固定对象销毁协议"| E
    E -->|"任务若可被抢占怎么办"| F
    F -->|"加入idle、乱序和慢路径"| G
    G -->|"拆开总图逐模块追源码"| H
    H -->|"普通Tree RCU之外还有什么"| I
    I -->|"怎样安全应用和验证"| J
```

默认阅读顺序就是文件编号，但上面的家族分流是进入任一版本化源码前的选择门。P03、P04 不再被放到子系统源码之后：先会正确使用和描述对象生命周期，才能判断后续源码到底在保护哪一个时间窗口。P05/P06 与 P07/P08 成对出现，每种模式都先讲具体反例和证明，再讲字段与调用链。

按阅读目的可以使用三条路线：

- **首次建立完整机制：** P01～P10 → P22 → P25～P28，先形成问题、证明、统一状态、实现边界和工程选择的闭环；
- **深入 Tree RCU 源码：** 完成 P01～P10 后阅读 P11～P21，沿 P10 的统一周期逐个放大子状态机；
- **比较实现家族：** 在 P22 之后阅读 P23、P24，比较私有域、特殊任务读者和单 CPU 简化模型。

三条路线共享同一套前置结论，不产生第二份正文。首次阅读遇到 P10 时可以先抓住角色、完整周期、状态拓扑和通知路径，再回看具体状态与特殊上下文。

## 1.2\_阶段一\_从现实问题到最小使用闭环

### 1.2.1\_为什么需要RCU

[为什么需要 RCU](P01_为什么需要_RCU.md)以互斥锁、读写锁和“只原子换指针”三种方案为同一场景基线，解释共享写热点、旧指针 UAF 和成本转移。

- 前置：基本锁、缓存一致性和指针概念。
- 本篇解决：RCU 为什么值得存在，正确性成本被移到了哪里。
- 留给下一篇：如果不看 Linux API，怎样从约束推出完整机制。

### 1.2.2\_抽象机制推演

[RCU 抽象机制推演](P02_RCU_抽象机制推演.md)用路由版本更新的代码现场，依次推出不可见构造、发布边界、旧 reader 集合、GP 和退休结果交付。

- 前置：P01 的锁方案与对象删除问题。
- 本篇解决：GP 等谁；已排队但尚未读取入口的未来 reader 为什么不属于旧集合。
- 留给下一篇：这些抽象动作对应哪些 Linux API，裸指针究竟是什么。

### 1.2.3\_通用API与最小闭环

[RCU 通用 API 与最小使用闭环](P03_RCU_通用API与最小使用闭环.md)给出完整 reader、同步 writer、异步 writer、模块退出代码，并明确 `rcu_dereference()` 返回同一对象地址，不复制对象。

- 前置：P02 的发布和 GP 边界。
- 本篇解决：接口顺序、同步/异步回收、晚到 reader、指针逃逸和第二入口误用。
- 留给下一篇：需要把对象或子块带出读区时，谁继续提供生命期。

## 1.3\_阶段二\_RCU与对象所有权组合

[RCU、kref 与复合对象生命周期](P04_RCU_kref与复合对象生命周期.md)不再把 `kref` 当 RCU 内建适配。它先区分 RCU、kref 和对象模块三方职责，再给出三种不能互相套用的完整代码模型：

1. 单个 RCU 对象在读侧内取得长期 kref；
2. 共享入口持有一份跨 GP 的发布引用；
3. 一个 RCU 版本根拥有多个分别带 kref 的分散数据块，GP 后由根逐块 `put`。

- 前置：P03 已经掌握 RCU 临时访问范围。
- 本篇解决：RCU 如何与 kref “牵手”、为什么二者彼此无感、谁是唯一销毁出口、读多时怎样避免逐块 get/put。
- 留给下一篇：不依赖对象地址和引用计数，RCU 自己怎样证明旧 reader 已经结束。

## 1.4\_阶段三\_非抢占式Tree\_RCU

这里的“非抢占式 Tree RCU”是对普通 Tree RCU 一个具体配置分支的命名，不表示非抢占式 RCU 与 Tree RCU 是同义词。本阶段只建立这一分支的 CPU 证明和源码模型；完整实现家族统一到 [P22 的三个正交维度](P22_RCU_实现家族与内核配置.md#22.2_三个正交维度)比较。

### 1.4.1\_问题与证明模型

[非抢占式 Tree RCU 的问题与证明模型](P05_非抢占式_Tree_RCU_问题与证明模型.md)固定 CPU0 writer、CPU1 旧 reader、CPU2 晚到 reader 的现场，穷举 GP 开始瞬间的 CPU 状态。

- 前置：P02 的时间边界与 P03 的使用协议。
- 本篇解决：`rcu_read_lock()` 为什么不必登记 reader 身份；一次合法 QS 为什么能排除该 CPU 上的既有旧 reader；未来 reader 为什么无需等待。
- 留给下一篇：QS 债务实际存在哪里，怎样从本地状态传播到根并唤醒 writer。

### 1.4.2\_源码同步机制

[非抢占式 Tree RCU 源码同步机制](P06_非抢占式_Tree_RCU_源码同步机制.md)把同一周期划分为 S0～S10，映射到 `rcu_state.gp_seq`、`rcu_node.qsmask`、`rcu_data.cpu_no_qs`、scheduler、context tracking 与 callback completion。

- 前置：P05 的 CPU 证明不变量。
- 本篇解决：GP 请求、初始化、CPU 感知、QS 锁存、异步上报、树形清位、cleanup 与等待者唤醒的完整源码链。
- 留给下一篇：若旧 reader 能在临界区内被换出，单靠 CPU QS 为什么失效。

## 1.5\_阶段四\_抢占式Tree\_RCU

### 1.5.1\_问题与任务跟踪模型

[抢占式 Tree RCU 的问题与任务跟踪模型](P07_抢占式_Tree_RCU_问题与任务跟踪模型.md)让已取得 `old_obj` 的任务被高优先级任务抢占，并在另一 CPU 恢复。

- 前置：P05/P06 的 CPU 债务模型。
- 本篇解决：为什么必须把“CPU 债务”和“任务债务”拆开；债务怎样在 context switch 时从本地执行现场转入共享登记；为什么 PREEMPT_RCU 不等于允许主动睡眠。
- 留给下一篇：任务字段、链表边界和最终 unlock 怎样兑现这次状态归属转移。

### 1.5.2\_源码同步机制

[抢占式 Tree RCU 源码同步机制](P08_抢占式_Tree_RCU_源码同步机制.md)追踪 `task_struct.rcu_read_lock_nesting/rcu_blocked_node`、叶节点 `blkd_tasks/gp_tasks`、CPU `qsmask` 和 `rcu_report_unblock_qs_rnp()`。

- 前置：P07 的两类债务和迁移场景。
- 本篇解决：GP 开始前后被抢占的任务如何纳入；CPU 位已清时任务为何仍阻塞节点；迁移后的 unlock 怎样回到原叶节点并恢复树形传播。
- 留给下一篇：CPU user/idle、缓存一致性、内存乱序和长时间不报告如何完善两种基础模式。

## 1.6\_阶段五\_现实约束与统一全景

### 1.6.1\_硬件与运行约束

[RCU 机制完善：硬件与运行约束](P09_RCU_机制完善_硬件与运行约束.md)用共享写热点、抢占迁移、EQS、分层汇聚、迟延参与者和内存乱序逐个检验朴素模型。缓存一致性、访问原子性和 release/acquire 的完整教程留在各自权威专题，本篇只保留 RCU 特有因果和链接。

- 前置：P05～P08 已分别建立 CPU 债务和任务债务的证明与源码模型。
- 本篇解决：真实硬件和运行环境会击穿哪些朴素假设，每个反例要求增加什么状态、通信路径或约束。
- 留给下一篇：成熟 Tree RCU 把这些新增能力放在哪些任务、CPU、节点和全局地址中，又怎样完成一轮统一通信。

### 1.6.2\_统一状态与通知总图

[Tree RCU 统一状态与通知总图](P10_Tree_RCU_统一状态与通知总图.md)在读者已经分别理解非抢占和抢占模式后，把每任务、每 CPU、每节点和全局状态放进一条端到端周期，比较正常报告、共享状态观察、FQS、IPI、boost、callback 与唤醒路径。

P09 的问题是“真实环境又击穿了哪些假设”；P10 的问题是“成熟 Tree RCU 把新增状态和通信放在什么位置”。

- 前置：P09 已把抢占迁移、EQS、分层汇聚、迟延参与者和内存顺序转成实现需求。
- 本篇解决：六组状态怎样共同推进一轮 GP，正常上报、共享登记、远端催促、回调和唤醒分别由谁承担。
- 留给下一篇：统一总图中的拓扑、GP、QS、汇聚、慢路径和回调子状态机怎样逐个映射到源码。

## 1.7\_阶段六\_Tree\_RCU实现纵深

以下章节不是孤立字段词典。每篇应沿 P10 的同一周期放大一个子状态机，并给出场景、代码/trace 入口、状态所有权和与前后阶段的接口。

1. [P11 初始化、拓扑与执行上下文](P11_Tree_RCU_初始化_拓扑与执行上下文.md)：启动时如何把 CPU 映射到叶节点，GP、core、softirq 与 callback 由谁执行。
   - 前置：P10 已给出任务、CPU、节点、全局状态和执行者的统一关系。
   - 本篇解决：启动阶段怎样建立节点树、每 CPU 归属和各类执行上下文。
   - 留给下一篇：拓扑建立后，多个同步者和回调请求怎样汇聚成物理 GP。
2. [P12 GP 请求与全局生命周期](P12_Tree_RCU_GP请求与全局生命周期.md)：先定义 GP、GP 请求、物理 GP、GP 代际、GP kthread 和完成发布，再解释多个 callback/同步者怎样合并需求，长期 GP kthread 如何创建、睡眠、开始、等待和 cleanup。需要从内核启动代码追踪这条线程的读者，可直接进入 [Linux 6.12 启动链与 GP kthread 创建源码实现](../../../../../research/source_reading/rcu/source_explanations/P05_Linux_6.12_Tree_RCU_GP全局生命周期源码实现.md#5.5.1_先从内核启动链定位early_initcall)。
   - 前置：P02 已定义抽象 GP；P11 只建立执行者位置，不再假设读者已经理解 GP kthread 生命周期。
   - 本篇解决：GP 专有名词各指什么，请求、物理 GP 和结果交付怎样分层，长期线程与单轮 GP 怎样区分，并发请求怎样共享代次。
   - 留给下一篇：GP 建立等待集合以后，CPU 怎样产生可以清偿本轮债务的 QS/EQS 证据。
3. [P13 QS、EQS 与 Context Tracking](P13_Tree_RCU_QS_EQS与Context_Tracking.md)：scheduler QS 与 user/idle watching 证明怎样互补。
   - 前置：P12 已建立一轮物理 GP 的生命周期和等待条件。
   - 本篇解决：调度、用户态和 idle 路径怎样产生不同形式的本地静止态证据。
   - 留给下一篇：分散在各 CPU 的证据怎样进入节点树并汇聚为全局完成。
4. [P14 rcu_node 树与分层汇聚](P14_Tree_RCU_rcu_node树与分层汇聚.md)：局部位图、节点锁和 `grpmask` 怎样降低全局争用。
   - 前置：P13 已说明 CPU 本地证据何时成立。
   - 本篇解决：本地等待位、节点锁和父子掩码怎样逐层清除，同时避免一个全局热字。
   - 留给下一篇：某些参与者迟迟不报告时，系统怎样催促和诊断而不猜测安全。
5. [P15 force-QS、迟延与 Stall](P15_Tree_RCU_force_QS迟延与Stall.md)：正常进展不足时如何扫描、催促和诊断，为什么绝不猜测安全。
   - 前置：P14 已建立正常证据沿节点树汇聚的路径。
   - 本篇解决：正常上报不足时怎样逐级扫描、催促和报告 stall，以及这些路径为什么只改善活性。
   - 留给下一篇：若调用者主动要求更短等待，怎样用更高系统扰动换取 expedited GP。
6. [P16 Expedited GP](P16_Tree_RCU_Expedited_GP.md)：低延迟请求改变哪条通知路径，为何成本更高。
   - 前置：P15 已区分正常进展、强制催促和 stall 诊断。
   - 本篇解决：加速 GP 怎样选择 CPU、发送 IPI、合并请求并维持与普通 GP 相同的安全条件。
   - 留给下一篇：无论 GP 怎样完成，异步 callback 怎样绑定目标代次并等待获得执行资格。
7. [P17 rcu_segcblist 回调状态机](P17_Tree_RCU_rcu_segcblist回调状态机.md)：callback 怎样绑定目标 GP 并在分段间推进。
   - 前置：P12 和 P16 已说明普通与加速 GP 怎样推进代次。
   - 本篇解决：callback 怎样从 NEXT 绑定目标 GP，并在分段状态中推进到 DONE。
   - 留给下一篇：具备执行资格的回调由谁实际调用，怎样避免一次成熟过多回调独占 CPU。
8. [P18 回调执行、批处理与限流](P18_Tree_RCU_回调执行_批处理与限流.md)：DONE 回调由哪个上下文执行，如何避免 callback 风暴独占 CPU。
   - 前置：P17 已建立 callback 获得执行资格之前的分段状态机。
   - 本篇解决：DONE 回调怎样被提取、锁外执行、批处理和限流。
   - 留给下一篇：隔离 CPU 不适合承担回调执行时，生产、等待 GP 和执行职责怎样卸载。
9. [P19 NOCB 回调卸载](P19_Tree_RCU_NOCB回调卸载.md)：bypass、GP kthread 与 CB kthread 如何转移 callback 负载。
   - 前置：P18 已明确普通 CPU 上的回调批处理和执行成本。
   - 本篇解决：NOCB 怎样通过 bypass、GP kthread 和 CB kthread 转移回调负载而不建立第二套 GP 语义。
   - 留给下一篇：等待旧读者、等待一个回调以及等待此前全部回调分别需要什么完成条件。
10. [P20 同步等待与 rcu_barrier](P20_Tree_RCU_同步等待与rcu_barrier.md)：等旧 reader、等本次 callback、等此前全部 callback 是三种什么条件。
    - 前置：P17～P19 已区分回调代际、执行资格、实际执行和 NOCB 卸载。
    - 本篇解决：`synchronize_rcu()`、单个 callback 完成和 `rcu_barrier()` 等待的对象为什么不同。
    - 留给下一篇：CPU 在 GP 或回调仍未完成时下线，参与集合与未执行工作怎样安全迁移。
11. [P21 CPU 热插拔与回调迁移](P21_Tree_RCU_CPU热插拔与回调迁移.md)：CPU 离线怎样改变参与集合，又怎样保留 blocked task 与未执行 callback。
    - 前置：P14 已建立节点等待集合，P17～P20 已建立回调和等待协议。
    - 本篇解决：CPU 上下线怎样改变当前与下一轮参与集合，并保留被抢占任务和未执行 callback。
    - 留给下一篇：完整 Tree RCU 之外，读者定义、域和部署条件改变时应选择哪种实现家族。

## 1.8\_阶段七\_实现家族与域边界

1. [RCU 实现家族与内核配置](P22_RCU_实现家族与内核配置.md)：先判断正在讨论哪种 reader 定义，再选择 API 和配置。
   - 前置：P05～P21 已建立普通 Tree RCU 的证明、状态和执行模型。
   - 本篇解决：读者定义、域、CPU 数量和 Kconfig 怎样共同决定实现家族，而不是由 API 名字直接决定。
   - 留给下一篇：需要显式私有域且读者可以阻塞时，为什么要改用 SRCU 的双 index 记账。
2. [SRCU 私有域与双 index 状态机](P23_SRCU_私有域与双_index_状态机.md)：需要显式域和可阻塞 reader 时，两期计数怎样替代普通 Tree RCU 的 CPU/任务证明。
   - 前置：P22 已把实现选择拆成读者定义、域和部署条件。
   - 本篇解决：SRCU 怎样用私有域和双 index 状态机等待可阻塞、可迁移读者。
   - 留给下一篇：等待任务执行轨迹或运行在单 CPU 环境时，为什么又不能套用 Tree RCU 或 SRCU 的证明。
3. [Tasks RCU 与 Tiny RCU 实现边界](P24_Tasks_RCU与Tiny_RCU实现边界.md)：任务级特殊读者和单 CPU 简化为何不能套用普通 Tree RCU 结论。
   - 前置：P22/P23 已区分普通 Tree RCU 与显式 SRCU 域的读者定义。
   - 本篇解决：Tasks RCU 家族和 Tiny RCU 分别改变了等待对象、探测方式与部署前提。
   - 留给下一篇：面对真实驱动和子系统对象，怎样依据入口、访问窗口和所有权拓扑选择生命周期模板。

## 1.9\_阶段八\_应用\_验证与选择

1. [RCU 驱动与子系统应用模式](P25_RCU_驱动与子系统应用模式.md)：设备链表、状态表、单对象上下文和复合配置快照怎样选择生命周期模板。
   - 前置：P03/P04 已建立使用与生命周期闭环，P22～P24 已说明实现家族边界。
   - 本篇解决：真实驱动和子系统怎样按对象入口、读侧窗口和分配拓扑选择应用模板。
   - 留给下一篇：代码写出以后，类型系统和运行时检查能够证明哪些协议，不能证明哪些生命周期事实。
2. [RCU 类型语义、Sparse 与 Lockdep](P26_RCU_类型语义_Sparse与Lockdep.md)：`__rcu`、访问器和运行时合法性检查能证明什么、不能证明什么。
   - 前置：P25 已给出真实对象的应用与拆除模式。
   - 本篇解决：Sparse 怎样通过 `__CHECKER__`、address space 与 `rcu_check_sparse()` 检查源码，四个 RCU map 又怎样在对应配置下形成 Lockdep 运行时影子状态，以及静态类型、动态上下文和对象生命期三层证据为何不能互相替代。
   - 动手验证：先在本篇用 `make C=1/C=2` 制造并识别 address-space 错误；再到 [Lockdep 配置、亲手实验与报告解读](../lockdep/P08_配置_亲手实验与报告解读.md#8.1_先把使用资格变成环境检查)确认运行资格，并完成[锁顺序反转与报告解读实验](../../../../../labs/kernel/lockdep/P01_锁顺序反转与报告解读/README.md#1.1_本实验把哪条推理交给读者完成)。通用实验只证明 Lockdep 运行链有效，RCU 结论还必须单独确认 `CONFIG_PROVE_RCU` 与目标 RCU 路径覆盖。
   - 交叉阅读：Lockdep 通用机制、查询语义和工程选择转入 [Lockdep 专题大纲](../lockdep/大纲.md#1.1_专题定位)；RCU 正文只保留子系统适配边界。
   - 留给下一篇：当系统已经出现 UAF、stall 或卸载竞态时，怎样把对象时间线、日志、trace 和动态检查器串成诊断流程。
3. [RCU 调试、验证与集成误用](P27_RCU_调试验证与集成误用.md)：用 rcutorture、trace、KASAN、lockdep 和 stall 日志区分协议错误与活性问题。
   - 前置：P25/P26 已明确正确应用协议和静态、动态检查能力。
   - 本篇解决：怎样区分裸指针逃逸、callback 未完成和旧读者不退出，并选择对应观察证据。
   - 留给下一篇：协议使用正确仍不代表发布顺序正确，最终还要怎样核对内存序与机制选型。
4. [RCU 内存序、误用与选择边界](P28_RCU_内存序_误用与选择边界.md)：把锁、refcount、seqcount、SRCU 与 RCU 放回同一选择表，不用“无锁更快”代替取舍。
   - 前置：前文已经建立 RCU 的使用协议、实现证明、变体、应用和诊断方法。
   - 本篇解决：发布/取得、GP、SRCU 域和内存压力分别具有什么边界，以及怎样在同一场景中选择 RCU 或替代机制。
   - 留给后续实践：把选型核对表应用到具体子系统，并通过相应源码导读和实验验证版本化结论。

## 1.10\_实验与版本化源码证据

- [晚到读者与抢占读者的对象回收实验](../../../../../labs/kernel/rcu/P01_晚到读者与抢占读者/README.md)：同一模块观察“任务存在但未读旧入口”和“已取旧指针后被抢占”两种完全不同的集合归属。
- **阅读导航：** [Linux 6.12 RCU 源码总阅读索引](../../../../../research/source_reading/rcu/navigation/P01_Linux_6.12_RCU源码总阅读索引.md#1.2_第一步必须先判断正在读哪一种RCU)先按 reader、保护域、主动阻塞和部署条件分流，再维护各模块阅读顺序。
- **模块概念导读：** [Linux 6.12 Tree RCU GP 全局生命周期模块源码概念导读](../../../../../research/source_reading/rcu/navigation/P06_Linux_6.12_Tree_RCU_GP全局生命周期模块源码概念导读.md#6.1_模块问题与版本边界)独立归纳 GP 请求、长期 GP kthread、init、FQS、cleanup 与完成交付。
- **模块概念导读：** [Linux 6.12 非抢占式 Tree RCU 模块源码概念导读](../../../../../research/source_reading/rcu/navigation/P02_Linux_6.12_非抢占式_Tree_RCU_模块源码概念导读.md#2.1_证据目标和配置边界)归纳同步等待桥、CPU QS 与树形汇聚主线。
- **模块概念导读：** [Linux 6.12 抢占式 Tree RCU 模块源码概念导读](../../../../../research/source_reading/rcu/navigation/P03_Linux_6.12_抢占式_Tree_RCU_模块源码概念导读.md#3.1_取证问题)归纳任务入队、`gp_tasks` 边界、迁移和最终解阻主线。
- **模块概念导读：** [Linux 6.12 Tasks RCU 与 Tiny RCU 模块源码概念导读](../../../../../research/source_reading/rcu/navigation/P04_Linux_6.12_Tasks_RCU与Tiny_RCU模块源码概念导读.md#4.1_Linux_6.12_Tasks_RCU与_Tiny_RCU模块源码概念导读)归纳任务扫描、holdout、Tasks Trace IPI 探测与单 CPU 回调边界。
- **模块概念导读：** [Linux 6.12 RCU Lockdep适配模块源码概念导读](../../../../../research/source_reading/rcu/navigation/P05_Linux_6.12_RCU_Lockdep适配模块源码概念导读.md#5.1_模块问题与实现所有权)区分通用 Lockdep 框架与 RCU 实例所有权，并组织读侧登记、held 查询和 callback 上下文三条调用链。
- **模块概念导读：** [Linux 6.12 Tree SRCU 模块源码概念导读](../../../../../research/source_reading/rcu/navigation/P07_Linux_6.12_Tree_SRCU模块源码概念导读.md#7.1_先分清Tree_RCU与Tree_SRCU)独立归纳私有域、可睡眠 reader、双 index 计数和域级 GP。
- **模块概念导读：** [Linux 6.12 Tree RCU 拓扑与 CPU 热插拔模块源码概念导读](../../../../../research/source_reading/rcu/navigation/P08_Linux_6.12_Tree_RCU_拓扑与CPU热插拔模块源码概念导读.md#8.1_本模块究竟解决什么问题)解释固定汇聚树、普通 current/next 集合、expedited ever-online 集合和 callback 迁移。
- **模块概念导读：** [Linux 6.12 Tree RCU force-QS 与 Stall 模块源码概念导读](../../../../../research/source_reading/rcu/navigation/P09_Linux_6.12_Tree_RCU_force_QS与Stall模块源码概念导读.md#9.1_为什么GP已经在等还要有force_QS)解释 watching 两阶段观察、urgent/resched、blocked task 与四类 stall 原因。
- **模块概念导读：** [Linux 6.12 Tree RCU Expedited GP 模块源码概念导读](../../../../../research/source_reading/rcu/navigation/P10_Linux_6.12_Tree_RCU_Expedited_GP模块源码概念导读.md#10.1_Expedited不是普通GP的加速档)独立组织 sequence、漏斗 leader/follower、CPU selection、IPI 与完成唤醒。
- **模块概念导读：** [Linux 6.12 Tree RCU 回调与 NOCB 模块源码概念导读](../../../../../research/source_reading/rcu/navigation/P11_Linux_6.12_Tree_RCU_回调与NOCB模块源码概念导读.md#11.1_GP完成为什么还不等于callback执行)串起 enqueue、GP 分类、DONE、实际执行、bypass 与 NOCB 双线程。
- **模块概念导读：** [Linux 6.12 Tree RCU 同步等待与 rcu_barrier 模块源码概念导读](../../../../../research/source_reading/rcu/navigation/P12_Linux_6.12_Tree_RCU_同步等待与rcu_barrier模块源码概念导读.md#12.1_等RCU至少有三种不同对象)区分 reader 完成、单等待者交付与系统既有 callback 实际执行。
- **源码讲解：** [Linux 6.12 RCU 公共接口与检查机制源码详解](../../../../../research/source_reading/rcu/source_explanations/P01_Linux_6.12_RCU_公共接口与检查机制源码详解.md#1.2_接口与源码索引)保存带中文 Doxygen 说明的公共接口、Sparse 与 Lockdep 实现。
- **源码讲解：** [Linux 6.12 Tree RCU GP 全局生命周期源码实现](../../../../../research/source_reading/rcu/source_explanations/P05_Linux_6.12_Tree_RCU_GP全局生命周期源码实现.md#5.5.1_先从内核启动链定位early_initcall)唯一展开从 `start_kernel()`、`kthreadd`、early initcall 分派到线程创建、请求、唤醒、主循环、init、FQS 调度循环、cleanup、poll 与 SRS 的完整生命周期；查单个符号时再进入其 [源码覆盖账本](../../../../../research/source_reading/rcu/source_explanations/P05_Linux_6.12_Tree_RCU_GP全局生命周期源码实现.md#5.2_源码符号覆盖账本)。
- **源码讲解：** [Linux 6.12 非抢占式 Tree RCU 关键函数源码实现](../../../../../research/source_reading/rcu/source_explanations/P02_Linux_6.12_非抢占式_Tree_RCU_关键函数源码实现.md#2.2_函数实现索引)保存带中文 Doxygen 说明的同步等待桥、CPU 感知、QS 和树形上报函数实现。
- **源码讲解：** [Linux 6.12 抢占式 Tree RCU 关键函数源码实现](../../../../../research/source_reading/rcu/source_explanations/P03_Linux_6.12_抢占式_Tree_RCU_关键函数源码实现.md#3.2_任务与节点的共享状态实现)保存带中文 Doxygen 说明的入队、退出与恢复传播实现。
- **源码讲解：** [Linux 6.12 RCU Lockdep适配层源码实现](../../../../../research/source_reading/rcu/source_explanations/P04_Linux_6.12_RCU_Lockdep适配层源码实现.md#4.1_实现所有权与读者目标)独立解释四个 RCU map 的声明、定义、key、wait type、事件配对、held 查询、callback 消费、配置退化和修改边界；通用 Lockdep 算法仍链接 Lockdep 专题的唯一实现。
- **源码讲解：** [Linux 6.12 Tree RCU 拓扑与 CPU 热插拔源码实现](../../../../../research/source_reading/rcu/source_explanations/P06_Linux_6.12_Tree_RCU_拓扑与CPU热插拔源码实现.md#6.2_源码符号覆盖账本)唯一展开 `rcu_init_one()`、CPU prepare/starting/dead、callback migration 与 CPUHP 收尾。
- **源码讲解：** [Linux 6.12 Tree RCU force-QS 与 Stall 源码实现](../../../../../research/source_reading/rcu/source_explanations/P07_Linux_6.12_Tree_RCU_force_QS与Stall源码实现.md#7.2_源码符号覆盖账本)唯一展开 watching save/recheck、叶扫描、FQS 节奏与 stall 一致性快照。
- **源码讲解：** [Linux 6.12 Tree RCU Expedited GP 源码实现](../../../../../research/source_reading/rcu/source_explanations/P08_Linux_6.12_Tree_RCU_Expedited_GP源码实现.md#8.2_源码符号覆盖账本)唯一展开 expedited sequence、漏斗、reset、selection、handler、wait/wake 与 API。
- **源码讲解：** [Linux 6.12 Tree RCU 回调与 NOCB 源码实现](../../../../../research/source_reading/rcu/source_explanations/P09_Linux_6.12_Tree_RCU_回调与NOCB源码实现.md#9.2_源码符号覆盖账本)唯一展开 `call_rcu()`、callback 分段推进、批处理、普通 core、bypass、NOCB 双线程和动态转换。
- **源码讲解：** [Linux 6.12 Tree RCU 同步等待与 rcu_barrier 源码实现](../../../../../research/source_reading/rcu/source_explanations/P10_Linux_6.12_Tree_RCU_同步等待与rcu_barrier源码实现.md#10.2_源码符号覆盖账本)唯一展开 `synchronize_rcu()` 分流、默认/SRS 等待边界和 barrier 哨兵证明。
- **源码讲解：** [Linux 6.12 Tree SRCU 源码实现](../../../../../research/source_reading/rcu/source_explanations/P11_Linux_6.12_Tree_SRCU源码实现.md#11.2_源码符号覆盖账本)唯一展开私有域对象、reader 双 index 累计账本、两次扫描、callback 需求漏斗、每域 GP work、同步等待和 SRCU barrier。

稳定知识正文说明跨版本机制；源码导读明确 Linux 6.12.20 的字段、函数和配置分支。Linux 5.10 的显著差异在相应源码章节中单列，不把 6.12 字段反向套用到旧版本。

## 1.11\_完整性验收

读完默认主线后，读者应能不靠口号回答：

1. writer 在什么时刻封闭 `old_obj` 的未来 reader 集合；
2. 已排队但尚未读取入口的任务为何不属于当前 GP；
3. 非抢占式模型为何按 CPU 等一次有效 QS，抢占式模型为何还要登记具体任务；
4. CPU 本地 QS、叶节点清位、父节点汇聚与根完成之间允许哪些异步窗口；
5. `synchronize_rcu()` 的等待对象保存在哪里，谁最终唤醒它；
6. RCU 与 kref 为什么彼此无感，单对象和多块旧照为什么有不同销毁顺序；
7. FQS、IPI、stall 与 boost 解决的是活性还是安全性；
8. 对一个真实对象，如何画出入口、临时访问、长期引用、回调和唯一最终销毁出口。

横向专题：[seqcount/seqlock](../sequence_counters/P01_seqcount_seqlock_读重试快照机制.md)、[kref 与 RCU](../../../object_lifetime/kref/P10_kref_与_RCU.md)、[Linux 内存顺序](../memory_ordering/大纲.md)。
