---
id: knowledge.linux.time_management.定时器简介.大纲
title: "驱动中的 时间问题 概述"
kind: mechanism
status: evolving
domains:
  - linux
  - kernel
---

# 第1章\_驱动中的\_时间问题\_概述

1.1 本书写作目的与适用读者
 1.2 驱动里典型的时间相关场景
 1.3 用户态时间 vs 内核时间的差异
 1.4 时间精度、上下文、可睡眠性三要素
 1.5 后续章节结构说明

# 第2章\_Linux\_时间基础与时间保持(timekeeping)框架速览

2.1 内核时间体系的历史与定位
 2.2 tick、HZ 与 `jiffies`
 2.3 clocksource / clockevent / timekeeping 的角色划分
 2.4 NO_HZ、高精度定时器配置对驱动的影响点
 2.5 本章小结与建议阅读路径

# 第3章\_时间表示与转换接口详解

3.1 相对时间与绝对时间的概念区分
 3.2 常用宏：`msecs_to_jiffies()`、`usecs_to_jiffies()`、`jiffies_to_msecs()`
 3.3 纳秒系与 `ktime_t`：`ktime_set()`、`ns_to_ktime()`
 3.4 不写转换宏的后果与可移植性问题
 3.5 与设备树时间属性的映射（如 `nxp,debounce-ms`）
 3.6 小结与调试要点

# 第4章\_基础定时器机制\_struct\_timer\_list

4.1 定时器在内核中的本质（软中断驱动的异步回调）
 4.2 数据结构与初始化方式（旧接口 vs 新接口）
 4.3 `add_timer()`、`mod_timer()`、`del_timer_sync()` 的语义
 4.4 回调与设备生命周期的竞态问题
 4.5 周期性定时器的正确写法
 4.6 可视化：timer 触发流程图（Mermaid）
 4.7 示例代码与常见错误
 4.8 小结

# 第5章\_高精度定时器\_hrtimer\_的驱动用法

5.1 为什么不是所有驱动都适合用 `timer_list`
 5.2 `hrtimer` 的核心结构与回调语义
 5.3 相对/绝对模式与周期重启
 5.4 上下文限制与不能睡的问题
 5.5 `hrtimer` + 工作队列的分层处理模式
 5.6 示例代码与调试
 5.7 小结

# 第6章\_基于工作队列的延迟执行\_delayed\_work

6.1 “需要睡”的延迟任务为什么该用工作队列
 6.2 `INIT_DELAYED_WORK()` / `schedule_delayed_work()` / 周期延迟
 6.3 退出与取消：`cancel_delayed_work_sync()` 的必要性
 6.4 与 `timer_list` / `hrtimer` 的取舍
 6.5 典型驱动场景：轮询、退避、重试
 6.6 示例代码与排错
 6.7 小结

# 第7章\_睡眠与超时调度接口

7.1 主动延时接口：`msleep()`、`usleep_range()`、`ssleep()`
 7.2 调度型超时：`schedule_timeout()` 系列
 7.3 等待队列超时：`wait_event_timeout()` / `wait_event_interruptible_timeout()`
 7.4 completion 超时：`wait_for_completion_timeout()`
 7.5 哪些上下文可用、哪些不可用（中断、BH、进程）
 7.6 示例代码与常见坑
 7.7 小结

# 第8章\_忙等待与短延时\_udelay()\_/\_ndelay()\_的边界

8.1 忙等待的实现思路与适用范围
 8.2 为何不能用 busy-wait 模拟长超时
 8.3 与寄存器轮询、总线就绪检测的组合写法
 8.4 SMP / PREEMPT / RT 下的注意事项
 8.5 小结

# 第9章\_时间与设备树/平台数据的对接

9.1 设备树中常见的时间类属性（`*-ms`、debounce、timeout）
 9.2 从 DT 到 jiffies / ktime 的转换策略
 9.3 以 `demo_led_key_int@0` 节点为例的时间参数接入
 9.4 驱动中动态调整定时参数的做法
 9.5 小结

# 第10章\_与\_devres\_驱动生命周期和\_PM\_的关系

10.1 为什么内核没有通用的 `devm_timer_create()`
 10.2 用 `devm_add_action_or_reset()` 对定时器做资源化回收
 10.3 定时器 + delayed_work 的收尾顺序
 10.4 挂起/恢复对定时器的影响与需要的补偿
 10.5 小结

# 第11章\_电源管理\_系统挂起与定时唤醒

11.1 系统挂起时定时器的行为特征
 11.2 RTC / alarm / PM QoS 与驱动的时间需求
 11.3 tickless/idle 对周期性驱动任务的影响
 11.4 写“既省电又能准时”的驱动策略
 11.5 小结

# 第12章\_常用模式与代码模板

12.1 周期性轮询硬件模板（低精度版/高精度版）
 12.2 中断中推迟到进程上下文的处理模板
 12.3 有超时的事务执行模板
 12.4 NFC/按键类去抖动的时间模板
 12.5 可直接粘贴的 helper 风格示例
 12.6 小结

# 第13章\_调试\_验证与常见陷阱

13.1 怎么确认定时器真的被调度了
 13.2 用 ftrace/tracepoints 看 timer/softirq
 13.3 “定时器还在跑但设备没了”类 crash 的定位
 13.4 时间回拨/时钟源异常导致的诡异超时
 13.5 性能与精度的取舍记录
 13.6 小结

# 第14章\_附录\_接口速查与对照表

14.1 不同上下文可用的时间/定时接口一览表
 14.2 精度 vs 开销 vs 可睡眠性对照表
 14.3 常见 SoC / 配置项对定时器的影响点
 14.4 推荐阅读与内核源码路径
 14.5 本专题总结

------

