---
id: knowledge.linux.synchronization.rcu.outline
title: "RCU 专题大纲"
kind: track
status: evolving
domains:
  - linux
  - kernel
topics:
  - synchronization
  - rcu
  - object_lifetime
---

# 第1章\_RCU\_专题大纲

## 1.1\_专题定位

RCU（Read-Copy-Update）是 Linux 内核中面向读多写少场景的通用同步机制。它将“读者如何安全观察对象”、“更新者如何发布新状态”和“旧对象何时可以回收”拆成不同的正确性问题。

本专题的权威机制正文放在当前目录；对象生命周期、数据结构和特定版本源码的内容仍保留在各自的本质归属中，本文只负责组织阅读顺序。

## 1.2\_建议阅读顺序

这套顺序刻意把 API 放在机制之后。读者先知道 RCU 在解决什么问题、硬件提供了什么、内核怎样接收并汇聚读者状态，之后接口名称才有含义。

```mermaid
flowchart LR
    A[问题<br/>锁竞争与 UAF] --> B[核心模型<br/>发布与延迟回收]
    B --> C[硬件基础<br/>一致性与内存序]
    C --> D[通知机制<br/>CPU／任务状态]
    D --> E[GP 汇聚<br/>回调放行]
    E --> F[变体<br/>Tree／SRCU／Tasks]
    F --> G[API 与模板]
    G --> H[驱动应用与边界]
```

### 1.2.1\_第一阶段\_从问题建立直觉

1. [为什么需要 RCU](P01_为什么需要_RCU.md)：从读写锁的共享写热点、删除对象的 UAF 难题和“允许旧读者读完”引出 RCU。
2. [RCU 核心概念与工作机制](P02_RCU_核心概念与工作机制.md)：正式区分读侧标记、发布、取消发布、宽限期和回收。

### 1.2.2\_第二阶段\_看清机器基础与通知机制

3. [RCU 的硬件基础与内存模型](P03_RCU_的硬件基础与内存模型.md)：解释 CPU 没有 RCU 单元，以及缓存一致性、原子访问、内存序究竟提供什么。
4. [Tree RCU 读侧与静止状态](P04_Tree_RCU_读侧与静止状态.md)：解释 `rcu_read_lock()` 如何留下状态，CPU/QS/EQS 和被抢占任务怎样通知 RCU。
5. [Tree RCU 宽限期与回调机制](P05_Tree_RCU_宽限期与回调机制.md)：解释通知如何经 `rcu_data`、`rcu_node` 汇聚成 GP 完成，并最终放行回调。

### 1.2.3\_第三阶段\_扩展到不同读者语义

6. [RCU 种类与内核配置](P06_RCU_种类与内核配置.md)：在理解普通 Tree RCU 后，再区分 Tree、PREEMPT、Tiny、SRCU、Tasks RCU 和 bh/sched 包装。
7. [SRCU 私有域与双 index 运行机制](P07_SRCU_私有域与双_index_运行机制.md)：理解私有域、显式读计数和可阻塞读侧为何需要另一套实现。

### 1.2.4\_第四阶段\_从会用到工程落地

8. [RCU API 速查](P08_RCU_API_速查.md)：统一查看读侧、发布、等待、回调和 SRCU 接口。
9. [RCU 模板、选型与核对](P09_RCU_模板_选型与核对.md)：把接口组合成完整调用链、时序图和最小模板。
10. [RCU 驱动应用模式](P10_RCU_驱动应用模式.md)：将模板落到设备表、状态表、异步路径和 kref 组合。

### 1.2.5\_第五阶段\_从多个角度复盘边界

11. [`__rcu` 类型语义与 Sparse 检查](P11_RCU_类型语义与_Sparse_检查.md)：从编译期检查角度发现错误的 RCU 指针访问。
12. [RCU 集成模式与常见误用](P12_RCU_集成模式与常见误用.md)：从锁、引用计数、工作队列和卸载流程审查组合边界。
13. [RCU 内存序与使用边界复盘](P13_RCU_内存序与使用边界复盘.md)：集中复盘选型、发布—取得契约和死锁边界。
14. [Linux 6.12 Tree RCU 与 SRCU 源码导读](../../../../research/source_reading/rcu/P01_Linux_6.12_Tree_RCU_与_SRCU_源码导读.md)：将机制模型对应到 NXP Linux 6.12.20 源码。

最后可横向对照[seqcount/seqlock](../sequence_counters/P01_seqcount_seqlock_读重试快照机制.md)、[kref 与 RCU](../../object_lifetime/kref/P10_kref_与_RCU.md) 和[哈希表的并发保护与 RCU](../../data_structures/哈希表_Hash_Table/P03_高级进阶与性能调优/P04_并发保护与RCU机制_多核下的读写博弈.md)。

## 1.3\_学习主线

阅读时始终围绕以下五个问题：

1. RCU 依赖哪些硬件与编译器机制，哪些正确性必须由内核软件完成？
2. 读侧入口、出口、调度和上下文跟踪分别留下什么状态，又在何时通知 RCU？
3. Tree RCU 如何从 CPU/QS/EQS 与被抢占任务中得出 GP 已完成？
4. 不同 RCU 实现分别定义什么读者集合，又等待谁？
5. 最后才问 API 如何表达上述运行机制。

## 1.4\_完成标准

完成本专题后，应当能够：

- 画出读者、更新者、宽限期和回收回调的时序关系。
- 区分 `rcu_assign_pointer()`、`rcu_dereference()`、`synchronize_rcu()`、`call_rcu()` 和 `kfree_rcu()` 的职责。
- 解释 `list_del_rcu()` 为什么不等于对象立即不可见，更不等于可以立即释放。
- 在普通 RCU、SRCU、seqcount/seqlock 和传统锁之间做出有依据的选择。
- 检查 RCU 与 kref、哈希表或链表组合时的指针生命周期错误。

