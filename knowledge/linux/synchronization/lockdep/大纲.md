---
id: knowledge.linux.synchronization.lockdep.outline
title: "Linux Lockdep 专题大纲"
kind: track
status: evolving
domains:
  - linux
  - kernel
topics:
  - synchronization
  - locking
  - lockdep
---

# 第1章\_Linux\_Lockdep专题大纲

## 1.1\_专题定位

本专题不把 Lockdep 写成一组字段、接口和告警名称，而是带着读者重新解决一个问题：**如果每一把锁都能正常加锁和解锁，系统为什么仍会死锁，我们又怎样在真实死锁尚未发生时证明这种风险？**

Lockdep 是 Linux 的动态锁协议验证器。它不替代 mutex、spinlock 或 rwsem，也不负责让一次加锁成功；它接收同步原语在约定时点上报的身份、取得、释放和 IRQ 状态事件，维护当前执行上下文的影子账本与跨时间的锁类历史，再检查递归、依赖环、IRQ 反转、等待上下文和持锁契约。

读完以后，读者不只应记住“Lockdep 会找环”，还应能够自行推导：

1. 为什么一次单任务执行也能暴露需要多个任务才会真正发生的 ABBA 死锁；
2. 为什么功能锁的 owner、等待队列和锁地址不足以承担全局证明；
3. 一次 `mutex_lock()` 怎样映射到实例、锁类、当前账本、依赖图和链缓存；
4. 为什么中断上下文可以使用某些自旋锁，却不能等待 mutex 或 completion；
5. 一条告警究竟由本次新事件和哪段历史共同推出；
6. 无告警在什么配置、覆盖和检查器生命状态下才是有限证据。

前置阅读是[锁机制专题](../locks/大纲.md)：至少要能区分 mutex 的可睡眠等待、自旋锁的忙等待，以及进程、softirq、hardirq 上下文。遇到版本化字段和函数时，再从本专题跳到 Linux 6.12.20 源码材料，不要求读者一开始就阅读 `lockdep.c`。

## 1.2\_贯穿场景与两条问题线

专题只使用一个虚构设备 `demo_device`，但把两类问题严格分开。

第一条是 **进程上下文的锁顺序问题**：

```text
配置路径：config_lock → state_lock
恢复路径：state_lock  → config_lock
```

两把锁都是 mutex。每条路径单独执行都能结束，两个任务以特定时序交错时才会互相等待。Lockdep 的目标是先分别收集这两条简单组件链，再在第二个方向出现时推出潜在 ABBA，不要求测试恰好重放真实卡死。

第二条是 **进程与硬中断共享队列的问题**：

```text
进程提交路径：本地硬中断开启时取得 queue_lock
硬中断路径：  取得同一个 queue_lock 更新完成队列
```

`queue_lock` 是自旋锁，不是 mutex。硬中断不能睡眠等待，但在普通非 PREEMPT_RT 内核中可以为短临界区取得自旋锁；若它打断了本 CPU 上持有同一自旋锁且没有屏蔽本地中断的进程路径，就会原地忙等，而被打断者永远无法运行到解锁。这个场景用于推导 IRQ-safe/unsafe 使用状态，不与 mutex ABBA 混成一个例子。

## 1.3\_认知主线

```mermaid
flowchart LR
    Q1["两个正确路径<br/>为何组合后会死锁"] --> M1["抽取等待组件链<br/>而不是等待真实卡死"]
    M1 --> Q2["当前持锁与历史顺序<br/>分别保存在哪里"]
    Q2 --> M2["实例身份＋当前账本<br/>锁类图＋使用状态"]
    M2 --> Q3["新取得事件<br/>怎样证明有风险"]
    Q3 --> M3["从后继反查前驱<br/>判断候选边是否闭环"]
    M3 --> Q4["IRQ、读锁、trylock<br/>为何改变阻塞关系"]
    Q4 --> M4["带类型的依赖<br/>与上下文状态传播"]
    M4 --> Q5["业务怎样使用结果<br/>测试怎样形成证据"]
    Q5 --> M5["断言、报告、覆盖<br/>容量与停检边界"]
```

这条主线刻意先建立抽象机制，再映射到 Linux 实现。`held_locks[]`、`lock_class_key`、`usage_mask` 和 `check_prev_add()` 只有在读者已经知道“为什么需要这类状态”以后才出现。

## 1.4\_章节契约

1. [为什么需要 Lockdep](P01_为什么需要_Lockdep.md)
   - 回答：两条各自正确的 mutex 路径如何形成 ABBA；为什么复现真实交错不是唯一验证办法。
   - 建立：锁原语的局部保证与模块锁协议的全局保证不是一回事。
   - 留下：若只执行简单路径，谁来保存当前前驱和跨时间历史？
2. [Lockdep 抽象模型与证明边界](P02_Lockdep_抽象模型与证明边界.md)
   - 回答：从“收集组件链”这一目标出发，最少需要哪些角色、状态和事件。
   - 建立：功能锁状态、当前影子状态、全局历史状态和验证器生命状态四层模型，以及 S0～S6 操作周期。
   - 留下：动态对象地址为什么不能直接永久充当全局图节点？
3. [锁实例、锁类、key 与 subclass](P03_锁实例_锁类_key与subclass.md)
   - 回答：成千上万个锁实例怎样共享协议结论；配置、`dep_map`、key 与 subclass 分别是什么。
   - 建立：实例用于当前查询，锁类用于跨实例推理，subclass 只表达经过证明的自然层级。
   - 留下：身份建立后，一次 acquire 到 release 究竟改写哪些状态？
4. [持锁账本、依赖图与状态闭环](P04_持锁账本_依赖图与状态闭环.md)
   - 回答：阻塞取得为什么在功能锁成功以前上报；失败怎样回滚；当前事实和历史结论怎样分离。
   - 建立：`held_locks[]`、链键、链缓存和锁类图的端到端状态流。
   - 留下：候选 `A → B` 到底要检查什么，为什么不是所有有向边都具有同样阻塞能力？
5. [递归、依赖环、IRQ 与读写规则](P05_递归_依赖环_IRQ与读写规则.md)
   - 回答：为什么添加 `A → B` 前必须从 B 搜索 A；硬中断、自旋锁和三类读写取得怎样扩展规则。
   - 建立：由真实等待可能性定义的强依赖路径，而不是“见环就报”的口号。
   - 留下：业务函数怎样消费当前账本，又怎样把自定义保护协议接入检查器？
6. [查询、断言、pin 与自定义原语接入](P06_查询_断言_pin与自定义原语接入.md)
   - 回答：查询、断言、连续持锁检查和事件上报为什么不能互相替代。
   - 建立：以调用者任务为中心的 API 选择模型和自定义原语接入核对表。
   - 留下：没有实体 mutex 的逻辑保护域如何复用 Lockdep？
7. [RCU 与子系统检查适配](P07_RCU与子系统检查适配.md)
   - 回答：RCU 为什么可以建立虚拟 lockdep map，业务锁条件 `c` 又怎样进入检查路径。
   - 建立：功能机制与检查机制并行、互不替代的子系统适配模型。
   - 留下：怎样确认配置、运行状态和测试路径真的让这些检查发生？
8. [配置、报告解读与验证方法](P08_配置_报告解读与验证方法.md)
   - 回答：如何构建最小实验、读取 splat、核对 `/proc/lockdep*` 并设计路径覆盖。
   - 建立：从报告中的新事件、历史反向链和当前账本回到代码协议的诊断方法。
   - 留下：一轮测试没有告警，能证明多少？
9. [成本、覆盖边界与工程选择](P09_成本_覆盖边界与工程选择.md)
   - 回答：链缓存、固定容量、`debug_locks` 停检、误报/漏报与工具分工。
   - 输出：能够判断何时启用 Lockdep、怎样记录有效证据，以及何时必须补 KCSAN、KASAN、Sparse、lockstat 或人工所有权证明。

## 1.5\_三种阅读方式

- **第一次建立模型：** 按 P01～P05 连续阅读，不跳进源码；先能亲手推导候选边、反向路径和 IRQ 递归场景。
- **准备在代码中使用：** 阅读 P01、P03、P06、P08、P09，重点核对配置、锁类身份、断言和覆盖。
- **准备阅读实现：** 先读 P01～P05，再从下节总阅读索引进入 `lockdep_map → held_lock → validate_chain() → check_prev_add()`，不要直接从某个报告函数逆猜整个机制。

## 1.6\_版本化源码入口

- **总阅读索引：** [Linux 6.12 Lockdep 源码导读](../../../../research/source_reading/lockdep/navigation/P01_Linux_6.12_Lockdep源码导读.md#1.1_基线与阅读目标)。
- **模块概念导读：** [身份与事件接入](../../../../research/source_reading/lockdep/navigation/P02_Linux_6.12_Lockdep身份与事件接入模块导读.md#2.1_模块问题)、[依赖图与规则引擎](../../../../research/source_reading/lockdep/navigation/P03_Linux_6.12_Lockdep依赖图与规则引擎模块导读.md#3.1_模块问题)、[查询适配与诊断](../../../../research/source_reading/lockdep/navigation/P04_Linux_6.12_Lockdep查询适配与诊断模块导读.md#4.1_模块问题)。
- **具体实现讲解：** [身份与锁类](../../../../research/source_reading/lockdep/source_explanations/P01_Linux_6.12_Lockdep身份与锁类源码实现.md#1.1_关联入口)、[取得释放与持锁账本](../../../../research/source_reading/lockdep/source_explanations/P02_Linux_6.12_Lockdep取得释放与持锁账本源码实现.md#2.1_关联入口)、[依赖图与规则引擎](../../../../research/source_reading/lockdep/source_explanations/P03_Linux_6.12_Lockdep依赖图与规则引擎源码实现.md#3.1_关联入口)、[查询注解与配置](../../../../research/source_reading/lockdep/source_explanations/P04_Linux_6.12_Lockdep查询注解与配置源码实现.md#4.1_关联入口)保存 Linux 6.12.20 的唯一函数体展开和仓库补充中文 Doxygen。

## 1.7\_与相邻专题的边界

- [锁机制专题](../locks/大纲.md)解释 mutex、spinlock、rwsem 等功能语义；本专题验证它们上报的协议事件。
- [中断机制专题](../../../kernel_subsystems/irq/中断机制简介)解释硬中断、下半部和线程化执行；本专题只分析 IRQ 上下文怎样改变锁依赖。
- [RCU 专题](../rcu/大纲.md)解释发布、读侧、宽限期和回收；本专题只解释 RCU 怎样消费 Lockdep 状态。
- [内存顺序专题](../../memory_ordering/大纲.md)解释 acquire/release 与屏障；Lockdep 的 `lock_acquire()` 是检查事件名，不等于 CPU acquire memory ordering。
- 数据竞争、越界/UAF、类型和对象生命期分别需要 KCSAN、KASAN、Sparse 及业务所有权证明，不能由 Lockdep 一项替代。
