------

# 目录

# 第 1 篇 · 基础与总体框架



## 第 1 章：GPIO 子系统全景与时间线（2.6 → 6.1+）

1.1 背景与目标：为何 6.1 适合作为稳定基线
 1.2 术语与边界：pin/pad/bank、GPIO line/chip、descriptor
 1.3 演进时间线：sysfs（废弃）→ chardev v1（4.8）→ chardev v2（5.10）→ 6.x 差异
 1.4 子系统关系：pinctrl/pinmux/pinconf ↔ gpiolib ↔ chardev ↔ input/LED/regulator
 1.5 典型误区与迁移心法（整数 API → 描述符 API）



## 第 2 章：环境与工具链（面向 6.1 LTS）

2.1 Kernel 配置矩阵：PINCTRL/GPIOLIB/GPIO_CDEV/IRQ/PM
 2.2 交叉编译与外部模块：Kbuild/Kconfig 模板
 2.3 RootFS 组装：busybox、udev、libgpiod 2.x 工具打包
 2.4 调试挂载与权限：debugfs、/dev/gpiochip* 的 udev 规则
 2.5 最小自检脚本：gpiodetect/gpioinfo/gpioget/gpioset/gpiomon



## 第 3 章：pinctrl / pinmux / pinconf 与 GPIO 的关系（实战）

3.1 引脚复用与电气配置的职责边界
 3.2 关键数据结构：pinctrl_desc/pinctrl_state/gpio_chip（含 gpio-ranges）
 3.3 设备树写法：pinctrl-names（default/sleep），状态切换原则
 3.4 最小可运行示例：两态 pinctrl + gpiod 输出 + PM 挂起/唤醒
 3.5 debugfs 验证：pinmux-pins / pinconf-pins
 3.6 常见错误与排查：复用冲突、电气不当、逻辑/物理映射



# 第 2 篇 · Consumer 与用户空间

## 第 4 章：GPIO 描述符 API（内核 Consumer 篇）

4.1 获取方式：required/optional/index/bulk 与 devm 化
 4.2 方向与电平：逻辑值 vs 原始值，active_low 语义
 4.3 上下文模型：gpiod_* vs gpiod_*_cansleep 的选择
 4.4 批量与错误处理模式，资源生命周期
 4.5 多线最小驱动 + sysfs 属性验证
 4.6 调试与验证 Checklist
 4.7 小结
 4.8（补充）`_cansleep` 系列函数详解：can_sleep 决策树、线程化中断模板、反模式清单



## 第 5 章：字符设备 ABI（v1/v2）与 libgpiod（用户态）

5.1 v1/v2 ABI 差异与回退策略
 5.2 工具法与“持有”语义（gpiodetect/gpioinfo/gpioget/gpioset/gpiomon）
 5.3 C API（libgpiod 2.x）：line_settings/line_config/request/事件缓冲
 5.4 输出/输入/事件最小程序（可编可跑）
 5.5 权限与并发：consumer 可见性、EBUSY 冲突定位
 5.6 与 pinctrl 的协同回溯（失败场景定位）



# 第 3 篇 · 中断与高级特性

## 第 6 章：GPIO 中断（IRQ）工程化

6.1 GPIO→IRQ：irqdomain、触发类型、极性关系
 6.2 线程化中断（threaded IRQ）与 `_cansleep` 协作
 6.3 去抖策略：硬件/控制器 set_debounce/软件时间窗
 6.4 唤醒：device_init_wakeup + enable_irq_wake + pinctrl sleep
 6.5 自定义驱动最小实现：devm_request_threaded_irq() + 统计接口
 6.6 验证与排错：/proc/interrupts、gpiomon、dmesg



## 第 7 章：标准消费者子模块总览与实战

7.1 gpio-keys：属性矩阵、事件链路与验证
 7.2 gpio-leds：default-state、trigger、多色/语义命名
 7.3 gpio-backlight：开关/亮度与面板/电源顺序
 7.4 regulator-fixed（GPIO 使能）：*-supply 依赖、boot-on、active-high
 7.5 gpio-mux：多路选择 GPIO 编码与 idle-state
 7.6 gpio-poweroff/restart：关机/重启触发与保持时间
 7.7 一页式 checklist（通用调试）



# 第 4 篇 · Provider（控制器）驱动编写

## 第 8 章：设备树 GPIO 绑定通用范式

8.1 `<name>-gpios`/flags 语法与含义
 8.2 gpio-line-names 与 gpio-ranges（对齐与命名）
 8.3 gpio-hog（上电默认态）
 8.4 命名规范：避免与标准属性同名；可读性规则
 8.5 常见坑库：极性不一致、复用覆盖、偏移混乱



## 第 9 章：GPIO 扩展器与桥接器（I²C/SPI/PMIC）

9.1 常见器件与差异（PCF8574/MCP23017/TCA9539/SX1509/MCP23S17）
 9.2 延迟与吞吐：批量 I/O、regmap 缓存
 9.3 中断聚合与级联：锁存/清中断策略
 9.4 `_cansleep` 约束与原子路径禁忌
 9.5 实战：扩展器 + 子模块组合（DTS + 验证）



## 第 10 章：性能、并发与实时性考量

10.1 延迟来源与预算模型（公式化表达）
 10.2 PREEMPT/RT 与调度：优先级/亲和/隔离核
 10.3 并发与内存一致性：spinlock/mutex/atomic/RCU
 10.4 用户态路径性能：批量请求、epoll、缓冲深度
 10.5 去抖与响应权衡：硬件/控制器/软件
 10.6 波形与 bit-banging 的边界（hrtimer、PWM/专用外设）
 10.7 扩展器与总线瓶颈：I²C/SPI、事务合并
 10.8 DVFS/Idle 的影响与 pm_qos
 10.9 度量方法：trace-cmd/ftrace/perf/cyclictest
 10.10 最佳实践与反模式



## 第 11 章：低功耗与唤醒协同

11.1 pinctrl 的 default/sleep/idle 设计模式
 11.2 休眠前安全电平与外设状态机
 11.3 唤醒源配置：IRQ wake、wakeup-source、autosuspend
 11.4 Bootloader → 内核的引脚接管一致性
 11.5 功耗测量与切换时序验证

# 第 5 篇 · 测试和迁移

## 第 12 章：调试与自测工具箱

12.1 调试面板总览：pinctrl/gpio(debugfs)、tracefs、dynamic_debug、/proc/interrupts
 12.2 快速体检：pinctrl 与 gpio 两步走
 12.3 dynamic_debug 精准开关日志
 12.4 trace-cmd/ftrace：路径测时与长尾定位
 12.5 libgpiod 工具：零代码复现
 12.6 kselftest/gpio-sim：无硬件的自动化回归
 12.7 标准化验证剧本与可视化
 12.8 Checklist 与小结



## 第 13 章：迁移与最佳实践（sysfs→chardev、v1→v2、整数 API→描述符 API）

13.1 迁移范围与时间线
 13.2 脚本/运维：sysfs 映射到 libgpiod 工具
 13.3 libgpiod v1→v2：API 心智模型变化与示例
 13.4 内核：整数 API→描述符 API（对照与模板）
 13.5 权限与部署：udev/容器策略
 13.6 迁移验证剧本与常见问题
 13.7 最佳实践清单



## 第 14 章：案例集与版型（可直接复用）

14.1 版型 A：板载 LED/按键/复位（子模块优先 + 复位钩子）
 14.2 版型 B：传感器电源 + 复位 + 唤醒（regulator + threaded IRQ）
 14.3 版型 C：gpio-mux 复用冲突化解（idle-state + reinit）
 14.4 版型 D：生产测试脚本与贴片后自检（line-names 驱动）
 14.5 SoC 移植要点对照（i.MX/RK/TI/Allwinner）
 14.6 调试 Checklist 与小结



## 第 15 章：FAQ / 附录

15.1 常见错误码与 dmesg 速查
 15.2 设备树片段 Cookbook（provider/consumer/hog/扩展器）
 15.3 Kconfig/Kbuild 最小模板
 15.4 Mermaid 图元与语法约定（Typora 兼容）
 15.5 术语表与参考链接（到 6.1+）

------

